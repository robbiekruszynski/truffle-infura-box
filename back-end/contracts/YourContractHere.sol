// If you’ve never heard of smart contacts, a way to think about them is; pieces of code that run on the blockchain (this can be local, test, or mainnet) that are guaranteed to produce the same results for everyone running them.

// example
// pragma solidity >=0.5.8;

// contract SimpleStorage {
//     uint256 storedData;

//     function set(uint256 x) public {
//         storedData = x;
//     }

//     function get() public view returns (uint256) {
//         return storedData;
//     }
// }